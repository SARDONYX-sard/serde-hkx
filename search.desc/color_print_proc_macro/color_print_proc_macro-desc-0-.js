searchState.loadedDescShard("color_print_proc_macro", 0, "This internal library provides the procedural macros …\nThe same as <code>eprint!()</code>, but parses color tags.\nThe same as <code>eprintln!()</code>, but parses color tags.\nThe same as <code>format!()</code>, but parses color tags.\nThe same as <code>print!()</code>, but parses color tags.\nThe same as <code>println!()</code>, but parses color tags.\nColorizes a string literal, without formatting the <code>format!</code>…\nThe same as <code>write!()</code>, but parses color tags.\nThe same as <code>writeln!()</code>, but parses color tags.\nRemoves all the color tags from the given string literal.")